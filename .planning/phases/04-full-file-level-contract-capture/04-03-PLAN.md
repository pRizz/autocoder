---
phase: 04-full-file-level-contract-capture
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - .planning/specs/contracts/non-ts/
  - .planning/specs/contracts/NON-TS-MATRIX.md
autonomous: true
requirements: ["FILE-03", "FILE-04", "FILE-05"]
user_setup: []
must_haves:
  truths:
    - "All canonical non-TypeScript artifacts (documentation, workflows, scripts, ops automation, config, planning docs) have contracts with equivalent schema depth."
    - "Non-TS contracts explicitly capture side effects, policy constraints, and acceptance checks suitable for regeneration."
  artifacts:
    - ".planning/specs/contracts/non-ts/"
    - ".planning/specs/contracts/NON-TS-MATRIX.md"
  key_links:
    - "NON-TS-MATRIX.md maps each non-TS ART ID to inventory path, contract path, and completion status."
---

<objective>
Capture full non-TypeScript contract lane for docs, operations, workflow, config, and planning artifacts.

Purpose: Satisfy FILE-04 by applying the same contract rigor to non-TypeScript artifacts as code files.
Output: Per-artifact non-TS contract files and non-TS matrix coverage evidence.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/specs/INVENTORY.md
@.planning/specs/FILE-SPEC-SCHEMA.md
@.planning/specs/FILE-SPEC-AUTHORING.md
@.planning/specs/contracts/BACKEND-RUNTIME-MATRIX.md
@.planning/phases/04-full-file-level-contract-capture/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build non-TS contract target list and matrix scaffold</name>
  <files>.planning/specs/contracts/NON-TS-MATRIX.md, .planning/specs/contracts/non-ts/</files>
  <action>Create `.planning/specs/contracts/non-ts/` and matrix rows for canonical `DOCUMENTATION`, `CI_AUTOMATION`, `OPERATIONS_SCRIPT`, `OPS_AUTOMATION`, `CONFIG_OR_MISC`, and `PLANNING_DOC` artifacts from `INVENTORY.md` (expected total: 88). Include ART ID, inventory path, contract file path, and status.</action>
  <verify>test -d .planning/specs/contracts/non-ts && rg -n "DOCUMENTATION|CI_AUTOMATION|OPERATIONS_SCRIPT|OPS_AUTOMATION|CONFIG_OR_MISC|PLANNING_DOC|ART-" .planning/specs/contracts/NON-TS-MATRIX.md && test "$(rg -n '^\\| ART-[0-9]{4} \\|' .planning/specs/contracts/NON-TS-MATRIX.md | wc -l | tr -d ' ')" = "88"</verify>
  <done>Non-TS matrix exists with 88 canonical targets and deterministic contract paths.</done>
</task>

<task type="auto">
  <name>Task 2: Author non-TS contract entries with equivalent rigor</name>
  <files>.planning/specs/contracts/non-ts/ART-*.md</files>
  <action>Author one contract file per non-TS target with full schema sections, including explicit side effects/policy behavior for workflows/scripts and precise acceptance checks for docs/config/planning artifacts.</action>
  <verify>test "$(find .planning/specs/contracts/non-ts -maxdepth 1 -type f -name 'ART-*.md' | wc -l | tr -d ' ')" = "88" && ! rg -L '^## Purpose$' .planning/specs/contracts/non-ts/ART-*.md && ! rg -L '^## Side Effects$' .planning/specs/contracts/non-ts/ART-*.md && ! rg -L '^## Error and Edge Behavior$' .planning/specs/contracts/non-ts/ART-*.md && ! rg -L '^## Acceptance Checks$' .planning/specs/contracts/non-ts/ART-*.md</verify>
  <done>All 88 non-TS targets have complete, schema-conformant contracts with equivalent detail level.</done>
</task>

<task type="auto">
  <name>Task 3: Validate non-TS lane traceability</name>
  <files>.planning/specs/contracts/NON-TS-MATRIX.md</files>
  <action>Ensure every non-TS contract file appears exactly once in matrix output and completion status is explicit for each row.</action>
  <verify>test "$(find .planning/specs/contracts/non-ts -maxdepth 1 -type f -name 'ART-*.md' | wc -l | tr -d ' ')" = "$(rg -n '^\\| ART-[0-9]{4} \\|' .planning/specs/contracts/NON-TS-MATRIX.md | wc -l | tr -d ' ')" && ! rg -n '\\| (Draft|Missing|TBD) \\|' .planning/specs/contracts/NON-TS-MATRIX.md</verify>
  <done>Non-TS lane matrix and contract files are 1:1 and complete.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] test -f .planning/specs/contracts/NON-TS-MATRIX.md
- [ ] test "$(rg -n '^\\| ART-[0-9]{4} \\|' .planning/specs/contracts/NON-TS-MATRIX.md | wc -l | tr -d ' ')" = "88"
- [ ] test "$(find .planning/specs/contracts/non-ts -maxdepth 1 -type f -name 'ART-*.md' | wc -l | tr -d ' ')" = "88"
- [ ] ! rg -L '^## Acceptance Checks$' .planning/specs/contracts/non-ts/ART-*.md
- [ ] git diff --name-only | rg -v '^\.planning/' returns no output
</verification>

<success_criteria>

- All tasks completed
- Non-TS lane covers docs/workflows/scripts/config/ops/planning artifacts with equivalent contract rigor
- Every contract includes error/edge behavior and acceptance checks
- No source implementation files modified

</success_criteria>

<output>
After completion, create `.planning/phases/04-full-file-level-contract-capture/04-03-SUMMARY.md`
</output>
