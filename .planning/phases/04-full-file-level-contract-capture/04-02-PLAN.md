---
phase: 04-full-file-level-contract-capture
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - .planning/specs/contracts/frontend-ui/
  - .planning/specs/contracts/FRONTEND-UI-MATRIX.md
autonomous: true
requirements: ["FILE-03", "FILE-05"]
user_setup: []
must_haves:
  truths:
    - "All canonical frontend source/config artifacts and UI e2e test artifacts have per-file contracts with explicit error/edge behavior."
    - "Frontend contracts define deterministic acceptance checks and direct dependencies needed for regeneration sequencing."
  artifacts:
    - ".planning/specs/contracts/frontend-ui/"
    - ".planning/specs/contracts/FRONTEND-UI-MATRIX.md"
  key_links:
    - "FRONTEND-UI-MATRIX.md maps each frontend ART ID to inventory path and generated contract file."
---

<objective>
Capture full UI/frontend file contracts using the finalized file-spec framework.

Purpose: Complete the full frontend contract lane, including UI source, frontend configuration, and UI e2e tests.
Output: Per-artifact frontend contract files and a frontend/UI matrix.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/specs/INVENTORY.md
@.planning/specs/FILE-SPEC-SCHEMA.md
@.planning/specs/FILE-SPEC-AUTHORING.md
@.planning/specs/contracts/BACKEND-RUNTIME-MATRIX.md
@.planning/phases/04-full-file-level-contract-capture/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build frontend/UI contract target list and matrix scaffold</name>
  <files>.planning/specs/contracts/FRONTEND-UI-MATRIX.md, .planning/specs/contracts/frontend-ui/</files>
  <action>Create `.planning/specs/contracts/frontend-ui/` and matrix rows for canonical `FRONTEND_SOURCE`, `FRONTEND_CONFIG`, and `ui/e2e/*` TEST artifacts from `INVENTORY.md` (expected total: 93). Include ART ID, inventory path, contract file path, and status.</action>
  <verify>test -d .planning/specs/contracts/frontend-ui && rg -n "FRONTEND_SOURCE|FRONTEND_CONFIG|ui/e2e|ART-" .planning/specs/contracts/FRONTEND-UI-MATRIX.md && test "$(rg -n '^\\| ART-[0-9]{4} \\|' .planning/specs/contracts/FRONTEND-UI-MATRIX.md | wc -l | tr -d ' ')" = "93"</verify>
  <done>Frontend/UI matrix exists with 93 canonical targets and deterministic contract paths.</done>
</task>

<task type="auto">
  <name>Task 2: Author frontend/UI contract entries</name>
  <files>.planning/specs/contracts/frontend-ui/ART-*.md</files>
  <action>Author one contract file per frontend/UI target with full schema sections, explicitly capturing interaction/state side effects, error/edge behavior, and acceptance checks suitable for reconstruction.</action>
  <verify>test "$(find .planning/specs/contracts/frontend-ui -maxdepth 1 -type f -name 'ART-*.md' | wc -l | tr -d ' ')" = "93" && ! rg -L '^## Purpose$' .planning/specs/contracts/frontend-ui/ART-*.md && ! rg -L '^## Error and Edge Behavior$' .planning/specs/contracts/frontend-ui/ART-*.md && ! rg -L '^## Acceptance Checks$' .planning/specs/contracts/frontend-ui/ART-*.md</verify>
  <done>All 93 frontend/UI targets have complete, schema-conformant contracts.</done>
</task>

<task type="auto">
  <name>Task 3: Validate frontend lane traceability</name>
  <files>.planning/specs/contracts/FRONTEND-UI-MATRIX.md</files>
  <action>Ensure every frontend contract file is represented exactly once in the matrix and that matrix status reflects completion for all rows.</action>
  <verify>test "$(find .planning/specs/contracts/frontend-ui -maxdepth 1 -type f -name 'ART-*.md' | wc -l | tr -d ' ')" = "$(rg -n '^\\| ART-[0-9]{4} \\|' .planning/specs/contracts/FRONTEND-UI-MATRIX.md | wc -l | tr -d ' ')" && ! rg -n '\\| (Draft|Missing|TBD) \\|' .planning/specs/contracts/FRONTEND-UI-MATRIX.md</verify>
  <done>Frontend lane matrix and contract files are 1:1 and marked complete.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] test -f .planning/specs/contracts/FRONTEND-UI-MATRIX.md
- [ ] test "$(rg -n '^\\| ART-[0-9]{4} \\|' .planning/specs/contracts/FRONTEND-UI-MATRIX.md | wc -l | tr -d ' ')" = "93"
- [ ] test "$(find .planning/specs/contracts/frontend-ui -maxdepth 1 -type f -name 'ART-*.md' | wc -l | tr -d ' ')" = "93"
- [ ] ! rg -L '^## Acceptance Checks$' .planning/specs/contracts/frontend-ui/ART-*.md
- [ ] git diff --name-only | rg -v '^\.planning/' returns no output
</verification>

<success_criteria>

- All tasks completed
- Frontend/UI lane covers all canonical frontend source, config, and UI e2e test artifacts
- Every contract includes error/edge behavior and acceptance checks
- No source implementation files modified

</success_criteria>

<output>
After completion, create `.planning/phases/04-full-file-level-contract-capture/04-02-SUMMARY.md`
</output>
