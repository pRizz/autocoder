---
phase: 04-full-file-level-contract-capture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/specs/contracts/backend-runtime/
  - .planning/specs/contracts/BACKEND-RUNTIME-MATRIX.md
  - .planning/specs/INDEX.md
autonomous: true
requirements: ["FILE-03", "FILE-05"]
user_setup: []
must_haves:
  truths:
    - "All canonical backend/runtime artifacts and root backend tests have per-file contracts with explicit error/edge behavior."
    - "Each backend/runtime contract includes regeneration-focused acceptance checks and direct dependency links."
  artifacts:
    - ".planning/specs/contracts/backend-runtime/"
    - ".planning/specs/contracts/BACKEND-RUNTIME-MATRIX.md"
  key_links:
    - "BACKEND-RUNTIME-MATRIX.md maps each backend/runtime ART ID to inventory path and contract file path."
---

<objective>
Capture full backend/runtime file contracts using the Phase 3 schema and conventions.

Purpose: Establish complete contract coverage for backend execution and root backend tests as the foundation for full-corpus capture.
Output: Per-artifact contract files + backend/runtime coverage matrix + index linkage.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/specs/INDEX.md
@.planning/specs/INVENTORY.md
@.planning/specs/FILE-SPEC-SCHEMA.md
@.planning/specs/FILE-SPEC-AUTHORING.md
@.planning/specs/FILE-SPEC-REVIEW.md
@.planning/phases/04-full-file-level-contract-capture/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build backend/runtime contract target list and matrix scaffold</name>
  <files>.planning/specs/contracts/BACKEND-RUNTIME-MATRIX.md, .planning/specs/contracts/backend-runtime/</files>
  <action>Create `.planning/specs/contracts/backend-runtime/` and generate matrix rows for all canonical `BACKEND_SOURCE` artifacts plus root `test_*.py` artifacts from `INVENTORY.md` (expected total: 56). Include columns for ART ID, inventory path, contract file path, and status.</action>
  <verify>test -d .planning/specs/contracts/backend-runtime && rg -n "BACKEND_SOURCE|test_.*\\.py|ART-" .planning/specs/contracts/BACKEND-RUNTIME-MATRIX.md && test "$(rg -n '^\\| ART-[0-9]{4} \\|' .planning/specs/contracts/BACKEND-RUNTIME-MATRIX.md | wc -l | tr -d ' ')" = "56"</verify>
  <done>Backend/runtime matrix exists with 56 canonical targets and deterministic contract output paths.</done>
</task>

<task type="auto">
  <name>Task 2: Author backend/runtime contract entries</name>
  <files>.planning/specs/contracts/backend-runtime/ART-*.md</files>
  <action>Author one contract file per matrix target using required schema sections: Artifact Identity, Purpose, Ownership, Inputs, Outputs, Side Effects, Direct Dependencies, Error and Edge Behavior, Acceptance Checks. Ensure dependency references use `ART-####` where known.</action>
  <verify>test "$(find .planning/specs/contracts/backend-runtime -maxdepth 1 -type f -name 'ART-*.md' | wc -l | tr -d ' ')" = "56" && ! rg -L '^## Purpose$' .planning/specs/contracts/backend-runtime/ART-*.md && ! rg -L '^## Error and Edge Behavior$' .planning/specs/contracts/backend-runtime/ART-*.md && ! rg -L '^## Acceptance Checks$' .planning/specs/contracts/backend-runtime/ART-*.md</verify>
  <done>All 56 backend/runtime targets have complete, schema-conformant contracts.</done>
</task>

<task type="auto">
  <name>Task 3: Publish backend/runtime linkage in spec index</name>
  <files>.planning/specs/INDEX.md</files>
  <action>Add a Phase 4 Full File Contracts section in `INDEX.md` with links to backend/runtime directory and matrix, labeling them as canonical lane artifacts for full-corpus coverage.</action>
  <verify>rg -n "Phase 4 Full File Contracts|BACKEND-RUNTIME-MATRIX|contracts/backend-runtime" .planning/specs/INDEX.md</verify>
  <done>Index exposes backend/runtime full-contract lane artifacts for downstream phases and audits.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] test -f .planning/specs/contracts/BACKEND-RUNTIME-MATRIX.md
- [ ] test "$(rg -n '^\\| ART-[0-9]{4} \\|' .planning/specs/contracts/BACKEND-RUNTIME-MATRIX.md | wc -l | tr -d ' ')" = "56"
- [ ] test "$(find .planning/specs/contracts/backend-runtime -maxdepth 1 -type f -name 'ART-*.md' | wc -l | tr -d ' ')" = "56"
- [ ] ! rg -L '^## Acceptance Checks$' .planning/specs/contracts/backend-runtime/ART-*.md
- [ ] git diff --name-only | rg -v '^\.planning/' returns no output
</verification>

<success_criteria>

- All tasks completed
- Backend/runtime contract lane covers all canonical backend + root backend test artifacts
- Each contract captures error/edge behavior and explicit acceptance checks
- No source implementation files modified

</success_criteria>

<output>
After completion, create `.planning/phases/04-full-file-level-contract-capture/04-01-SUMMARY.md`
</output>
